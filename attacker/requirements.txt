# Yucatan Slang Jailbreak Benchmark — Python attacker dependencies
litellm>=1.51.0          # unified client + LiteLLM Proxy compatibility
psycopg2-binary>=2.9.9   # PostgreSQL driver (jerga corpus + results)
python-dotenv>=1.0.1     # optional: load .env for local CLI runs
pyyaml>=6.0.1            # config/models.yaml loader
pydantic>=2.5.0          # JudgeVerdict schema validation
pytest>=8.0.0            # unit tests
