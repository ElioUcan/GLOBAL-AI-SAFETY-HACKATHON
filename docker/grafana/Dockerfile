FROM grafana/grafana:11.1.0

# PostgreSQL is a first-party, built-in Grafana datasource — no plugin install
# required. Provisioning wires it up from POSTGRES_* env vars (see compose.yml).

EXPOSE 3000
